import React from 'react';

import { Splash } from '.';

export default {
  title: 'V2/Splash',
  component: Splash,
};

const Template = (args) => <Splash {...args} />;

export const Default = Template.bind({});
