// export const calculateTokenPrice = val => {
//   const formatter = new Intl.NumberFormat('en-US', {
//     style: 'currency',
//     currency: 'USD',
//     minimumFractionDigits: 4,
//   });
//   const newVal = formatter.format(val);
//   return newVal;
// };

// function calculatePrice(
//   daiInPool,
//   covTokensInPool,
//   daiPrice,
//   covTokenWeight,
//   daiWeight,
// ) {
//   return (
//     daiInPool / ((covTokensInPool * daiWeight) / (covTokenWeight * daiPrice))
//   );
// }
